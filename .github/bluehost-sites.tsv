# local_path	remote_path (relative to the Bluehost SSH home dir, /home3/evraymon)
# Same Bluehost account/SSH key for all. rsync has no --delete — it only
# adds/updates, never removes files already on the server.
#
# CONTENT HUBS — publishing policy:
# A hub is ALWAYS served from GitHub Pages at
#   https://cabuzzard.github.io/dash/web/hub/{slug}/
# That is the home of every "pre-domain" (not-yet-connected) hub — do not try to
# route those to Bluehost. Add a `web/hub/{slug}  <domain>` line here ONLY once
# that hub's domain is actually connected to this hosting account and serving.
#
# The Bluehost target for a hub is /home3/evraymon/<domain>/ — the folder
# Bluehost auto-creates for the domain at registration and uses as the doc root
# once the domain is connected. So remote_path is just the domain name.
# Per hub, also: worker.js HUB_ORIGINS (apex + www) + index.html HUB_SITES[].domain.
# See CLAUDE.md "Content Hubs".
#
# Status 2026-09-01: creativeflowguitar.com is connected and serving. The other
# six hub domains are stuck behind Bluehost's auto-"WordPress service" park
# (root A record → 204.11.56.246, not the hosting IP 162.241.218.154) — they
# stay GitHub-Pages-only until that placeholder service is disconnected
# (Bluehost account → Domains → <domain> → Overview → Connected Services → Manage)
# or Bluehost support repoints the domain at the cPanel account. Add each one's
# line below when it's live.
web/sm-business-software-tools	public_html/webguyleadgeneration
web/hub/creative-flow-guitar	creativeflowguitar.com
