# local_path	remote_path (relative to the Bluehost SSH home dir, /home3/evraymon)
# Same Bluehost account/SSH key for all. rsync has no --delete — it only
# adds/updates, never removes files already on the server.
#
# ── CONTENT HUBS ──────────────────────────────────────────────────────────
# As of 2026-09-03 ALL 8 hubs are served from CLOUDFLARE PAGES, not Bluehost.
# One Pages project ("dash-hubs") holds every hub as a subdir and routes each
# custom domain to its subdir by Host header (web/hub/_worker.js). Deploy:
# .github/workflows/deploy-hub-pages.yml. Domains/DNS/email: see
# web/hub/CLOUDFLARE-ROUTING.md. Bluehost keeps the mail (MX stays on Bluehost).
# creativeflowguitar.com was the last holdout — migrated 2026-09-03; the old
# files under ~/creativeflowguitar.com stay on the server but the domain now
# points at dash-hubs.
web/sm-business-software-tools	public_html/webguyleadgeneration
